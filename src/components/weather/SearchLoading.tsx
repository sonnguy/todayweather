import { Spinner, SpinnerContainer } from "./style";

const SearchLoading = (props: any) => {
    return (
        <SpinnerContainer>
            <Spinner />
        </SpinnerContainer>
    );
}

export default SearchLoading;
