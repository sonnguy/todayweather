import { Spinner, SpinnerContainer } from "./style";

const SearchLoading = () => {
    return (
        <SpinnerContainer>
            <Spinner />
        </SpinnerContainer>
    );
}

export default SearchLoading;
